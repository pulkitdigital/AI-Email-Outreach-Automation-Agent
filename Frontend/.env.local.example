# Copy this file to Frontend/.env.local and fill in the real backend API URL.
# Next.js auto-loads .env.local from this project's own root — no other config needed.

# The Backend API's base URL. Local dev default shown; update after deploying Backend to Render.
NEXT_PUBLIC_API_URL=http://localhost:4000
